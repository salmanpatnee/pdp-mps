# Data Model: Manuscript Search by Reference Number

No changes to the existing data model are required for this feature. The `Manuscript` entity already contains the `reference_no` and `title` attributes, which are sufficient for implementing the search functionality.
